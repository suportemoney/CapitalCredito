App label: contratos_v2 (tabelas contratos_v2_* no MySQL).

=== Na VPS (com venv e MySQL) ===

  git pull
  pip install -r requirements.txt

  python manage.py migrate siape 0005
  python manage.py migrate contratos_v2
  python manage.py migrate siape 0007
  python manage.py migrate

Não é necessário makemigrations — o 0001_initial.py já está no repositório.

Se existir linha siape.0006 em django_migrations (versão antiga), remova-a antes de migrar.

Este clone local não tem banco; não rode migrate aqui.
